using UnityEngine;
using System.Collections;

public class GO_objeto : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 20);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
